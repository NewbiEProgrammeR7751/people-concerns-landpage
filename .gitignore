node_modules
dist
# .figma/make/site.json is a build input (imported by vite.config.ts) — keep it tracked.

# Secrets — the GitHub PAT for the Recent Activity widget lives here.
# Only .env.example is committed.
.env
.env.*
!.env.example
